doctype html
html
    head
        title InsToTer
        link(rel="stylesheet" href="//cdn.bootcss.com/normalize/5.0.0/normalize.min.css")
    body
        h1 Ins(tagram)-to-(Twit)ter

        - var ready = twitter && instagram
        if ready
            h2 Link your accounts first:
        else
            h2 Start

        ul
            li
                if twitter.status == 1
                    | Twitter Ready
                else
                    a(href="/instoter/twitter_login") Twitter
            li
                if instagram.status == 1
                    | Instagram Ready
                else
                    a(href="/instoter/instagram_login") Instagram
